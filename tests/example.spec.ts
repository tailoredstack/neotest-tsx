import { describe, it } from 'node:test';
import assert from 'node:assert';

describe('Spec File Tests', () => {
  it('should work in .spec.ts files', () => {
    assert.strictEqual(1 + 1, 2);
  });
});